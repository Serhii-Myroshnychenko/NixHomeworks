@model MVC.ViewModels.BasketViewModels.BasketIndexViewModel

@*<div class="row checkoutForm">
    <h2>Your shopping cart</h2>
    <h4>Here are the drinks in your shopping cart.</h4>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Selected amount</th>
                <th>Drink</th>
                <th class="text-right">Price</th>
                <th class="text-right">Subtotal</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var line in Model.CatalogBasketCars)
            {
                <tr>
                    <td class="text-center">@line.Id</td>
                    <td class="text-left">@line.Model</td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" class="text-right">Total:</td>
                <td class="text-right">
                    @Model.Total.ToString("c")
                </td>
            </tr>
        </tfoot>
    </table>

    <div class="text-right">
        <div class="btn-group ">
            
        </div>
    </div>
</div>
*@




@*<section class="esh-catalog-hero">
    <div class="container">
    </div>
</section>

<section class="esh-catalog-filters">
    <div class="container">
        <form asp-action="Index" asp-controller="Basket" method="post" >
            <label class="esh-catalog-label" data-title="type">
                <select asp-for="Total" asp-items="@(new SelectList(Model.CatalogBasketCars))" class="esh-catalog-filter"></select>
            </label>
            <input class="esh-catalog-send" type="image" src="~/images/arrow-right.svg" />
        </form>
    </div>
</section>

<div class="container">
    <div class="row">
        <br />
        @if(ViewBag.BasketInoperativeMsg != null)
        {
        <div class="alert alert-warning" role="alert">
            &nbsp;@ViewBag.BasketInoperativeMsg
        </div>
        }
    </div>

    @if (Model.CatalogBasketCars.Count() > 0)
    {
    <div class="esh-catalog-items row">
        @foreach (var catalogCar in Model.CatalogBasketCars)
            {
        <div class="esh-catalog-item col-md-4">
            <partial name="_cars" model="catalogCar" />
        </div>
            }
    </div>
    }
    else
    {
    <div class="esh-catalog-items row">
        THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
    </div>
    }
</div>
*@


@*<h1>Your Basket</h1>

<table class="table">
    <thead>
        <tr>
            <th>Car Model</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.CatalogBasketCars.Count() > 0)
        {
            @foreach (var item in Model.CatalogBasketCars)
            {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Model)</td>
                <td>@item.Price</td>
            </tr>
            }
        }
        else
        {
            <div>
                THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
            </div>
              
        }
    </tbody>
</table>

<h2>Total: @Model.Total.ToString("C")</h2>*@

<div class="container">
    <div class="row">
        <br />
        @if(ViewBag.BasketInoperativeMsg != null)
        {
        <div class="alert alert-warning" role="alert">
            &nbsp;@ViewBag.BasketInoperativeMsg
        </div>
        }
    </div>

    @if (Model.CatalogBasketCars.Count() > 0)
    {

    <div class="esh-catalog-items row">
        @foreach (var catalogCar in Model.CatalogBasketCars)
            {
        <div class="esh-catalog-item col-md-4">
            <partial name="_cars" model="catalogCar" />
        </div>
            }
    </div>
    <div>
        <h2>Total: @Model.Total.ToString("C")</h2>
    </div>

    }
    else
    {
    <div class="esh-catalog-items row">
        THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
    </div>
    }
</div>